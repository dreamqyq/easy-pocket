import React from 'react';
import { Layout } from 'components/Layout';

const Chart: React.FC = () => {
  return (
    <Layout>
      <div>Chart Page</div>
    </Layout>
  );
};

export default Chart;
